/**
 * Installed speaks to someone managing what is already on this machine, in
 * deliberate contrast to Browse — whose copy is the hero's own
 * `PLUGIN_HERO_COPY.description`, since the hero is what renders it. A second
 * copy of that string used to live here unused, and the two had already
 * drifted apart.
 */
export const PLUGINS_INSTALLED_DESCRIPTION =
  "The plugins installed on this bb machine. Turn one on or off, apply updates, or open it for settings and details.";
