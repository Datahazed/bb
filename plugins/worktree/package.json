{
  "name": "bb-plugin-worktree",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Provisions a new git worktree on an enrolled machine for each thread.",
  "engines": {
    "bb": ">=0.0"
  },
  "bb": {
    "name": "Worktree",
    "description": "Provisions a new git worktree on an enrolled machine for each thread.",
    "branding": {
      "icon": "GitBranch"
    },
    "server": "./server.ts",
    "app": "./app.tsx",
    "host": "./host.ts"
  },
  "keywords": [
    "bb-plugin"
  ],
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bb/host-workspace": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@get-bb/plugin-sdk": "workspace:*",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "react": "^19.0.0",
    "typescript": "npm:@typescript/typescript6@^6.0.2",
    "vitest": "^4.1.1"
  }
}
